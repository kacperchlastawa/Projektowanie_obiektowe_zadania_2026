version: '3.8'
services:
  php:
    image: kprzystalski/projobj-php:latest
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true
    entrypoint: ["tail", "-f", "/dev/null"]